# GloLaunch AI — 复赛提交材料

> **作品名称**：GloLaunch AI · AI 全链路跨境智能上新引擎
> **参赛场景**：场景一 · 智能上新（用 AI 实现从选品到上架的自动化）
> **一句话定位**：**Accio 帮没有货的人开店；GloLaunch AI 帮有货的人每天上架。**
> **技术底座**：阿里云百炼平台（Qwen 系列文本/视觉模型 + Wan2.7 生图 + TTS 语音合成）+ LangGraph 智能体编排

---

## 一、产品定位与竞品差异化

### 1.1 我们要解决的一线真问题

中国跨境电商卖家最主流的日常操作不是"从创意发明一个新品"，而是**从 1688 等货源平台搬运现成商品，快速上架到海外平台**。这条真实工作流存在四个一线痛点：

1. **内容不能直接卖**：1688 的详情图是中文、国内审美，直接搬到 Amazon 会死；
2. **Listing 不能直译**：直译的标题没有搜索流量，要按目标平台爆款公式改写；
3. **流程高频且重复**：卖家每天搬几个到几十个品，需要流水线工具而非一次性方案；
4. **AI 工具空转浪费**：多数 AI 上新工具假设用户没有素材、全量生成，但搬运场景素材是现成的——强制生成既慢又烧额度。

GloLaunch AI 由此推导出核心产品策略——**"搬运优先、AI 按需补给"**：

> 用户粘贴一个 1688 链接，系统自动解析商品 → 智能推荐目标平台站点 → 一键跑完 9 节点全链路：
> 属性识别 → 市场洞察 → 爆款对标 → Listing 改写 → 素材沿用/按需生成 → 带货视频 → 图片本地化 → 合规校验 → 发布包。
> 已有素材零生成消耗，只在真正缺的环节调用 AI。

### 1.2 与 Accio Work 的关系：生态补位，不是正面竞争

Accio Work 是阿里国际的企业级 AI Agent 平台，服务"没有货源的创业者从创意开始开店"。我们与之是**错位互补**关系：

- **Accio 是"创业时刻"的工具**——开店、选品、谈供应商，对同一个卖家一辈子发生几次；
- **GloLaunch AI 是"每天早晨"的工具**——搬运、上架、改标题、批量铺货，是卖家每天重复 50 次的高频作业。

一个形象的类比：**Accio 组建了一支管理公司的 AI 团队；GloLaunch AI 是这支团队里专门负责上新流水线的那条产线。** 未来它甚至可以是 Accio 编排调用的一个 Skill——我们已经对接 1688 开放平台官方 API，天然长在阿里生态里。

### 1.3 卖家就算在用 Accio，为什么还需要 GloLaunch AI（四个真实理由）

| # | 理由 | 说明 |
|---|------|------|
| 1 | **频率错位** | Accio 的通用任务空间不适合流水线式日常作业。我们按"每天用 50 次"设计：批量 CSV 上新、任务历史、断点续跑、一键搬运零手填 |
| 2 | **Listing 迭代维度** | 上架不是发一次就结束，卖家天天改标题测关键词。我们有 Listing 版本自动存档 + 字段级对比（标题字符数/五点增删改/关键词变化），通用平台无此垂直深度 |
| 3 | **内容生产深度** | "有货"不等于"有素材"。我们提供详情图文字本地化（中文图→目标语言图）、海外生活方式场景图按需补生成、TTS 配音带货视频素材包——Accio 建店流程不覆盖这一层 |
| 4 | **数据主权与私有化** | 开放技术栈（FastAPI + Vue + LangGraph），可私有化部署、可对接卖家自有 ERP；封闭 SaaS 永远给不了这个选项 |

### 1.4 能力对照表

| 维度 | Accio Work | GloLaunch AI |
|------|-----------|--------------|
| 起点 | 从"创意"出发（0→1 开店） | 从"已有商品链接"出发（1→100 上架） |
| 目标用户 | 无货源创业者、海外中小企业 | 已有供应链的存量跨境卖家 |
| 供应链 | 150 万供应商询价谈判（生态资源） | 1688 单品/批量搬运导入（官方 API 通道） |
| 内容生产 | 建店流程附带 | **真实生图 + 详情图本地化 + TTS 配音视频素材包（更深）** |
| Listing | 上架环节附带 | **多平台规范差异化 + 爆款公式 RAG 改写 + 版本对比（更深）** |
| 合规 | 经营合规（VAT/海关，100+ 市场） | 发布合规（标题字节/违禁词/认证要求逐项校验） |
| 工程形态 | 封闭 SaaS | 开源技术栈，可视化编排工作台，可私有化 |
| 卖家日常 | 通用任务空间 | 批量上新 / 断点续跑 / 任务历史 / 版本管理 |

**诚实声明**：市场洞察环节我们基于模型知识 + 自建爆款知识库（RAG），不具备阿里实时交易数据；架构已预留数据接入层，商业化阶段可接入卖家店铺后台与平台榜单数据。

### 1.5 评委可能提问的预备口径

| 可能的提问 | 回答要点 |
|-----------|---------|
| 你们和 Accio 什么区别？ | 1.2 节：错位互补，创业时刻 vs 每天早晨；我们愿做 Accio 生态的上新产线 |
| 创新性在哪里？ | "搬运优先、AI 按需补给"是从真实业务洞察推导出的架构决策：所有竞品假设用户从零开始、全量生成；我们假设用户已有商品与素材，AI 只补给真正缺的环节——搬运模式零生图消耗，上新耗时与算力成本大幅下降 |
| 为什么不做全链路（采购/物流/合规申报）？ | 高频刚需优先：把卖家每天重复的上新环节做深做透，比平铺全链路更有业务价值；其余环节属于 Accio 等生态产品的分工 |
| 市场数据从哪来？ | 模型知识 + 自建爆款知识库；预留数据接入层（见 1.4 诚实声明） |
| 商业落地怎么赚钱？ | 面向中小卖家的上新流水线工具（订阅制）+ 可私有化交付给大卖/ERP 厂商（被集成）；与 1688 货源生态天然协同 |
| 市场洞察、视频节点会不会越界到场景三/二？ | 见 1.6 对齐自检：市场洞察是单品上架前的一次性决策，非持续市场监控；视频是 Listing 附带的主图视频，非 KOL/投放素材。我们没做的才是场景二/三 |

### 1.6 赛道对齐自检：全部能力收敛于场景一

我们只申报场景一（AI 智能上新）。对照官方描述逐句自检，9 节点全部落在该赛道内：

| 场景一官方描述 | GloLaunch AI 对应能力 |
|---------------|----------------------|
| 从选品到上架，一键完成 | 1688 一键搬运 → 9 节点全链路，全程零手填 |
| AI 自动生成高转化商品主图与详情页 | AI 商品摄影（白底主图/场景图）+ 图片本地化（详情图中文→目标语言） |
| 智能适配多平台规则 | Listing 多平台规范差异化 + 平台合规逐项校验 |
| 批量完成 Listing 撰写与后台上架 | 批量 CSV 上新 + OAuth 直连发布（Amazon SP-API） |
| 将传统数天的流程压缩至分钟级 | 单品全链路分钟级，工作台实时进度与 ETA |

两个易被质疑的节点，边界明确：

- **市场洞察 ≠ 场景三（市场洞察）**：是单品上架前的一次性决策（定价区间/卖点/长尾词），产出直接喂给 Listing 改写；不做持续趋势监控、竞品追踪、供应链信号。
- **带货视频 ≠ 场景二（智能营销）**：产出 Listing 附带的主图/详情页视频，属于上架内容；不做 KOL 匹配触达、广告竞价、渠道投放。

**我们没做的才是场景二和场景三；做的恰是场景一的闭环。** 赛道内的深度不是越界。

---

## 二、产品功能与使用说明

### 2.1 两种入口

| 入口 | 适用场景 | 操作 |
|------|---------|------|
| **一键搬运（推荐）** | 1688 已有货源（主流场景） | 粘贴链接 → 自动解析标题/主图/价格 → 智能推荐平台站点（家居母婴偏好 Shopee 东南亚，其余默认 Amazon 美区）→ 一键启动全链路，全程零手填 |
| **手动上新** | 自有商品/无货源素材 | 上传商品图或纯文字描述 → 选择目标平台站点 → 启动（无素材时自动走 AI 全量生图） |

### 2.2 九节点全链路（LangGraph 编排）

| # | 节点 | 能力 | 关键设计 |
|---|------|------|---------|
| 1 | 商品属性识别 | 多模态视觉提取品类/材质/风格等结构化属性 | 全品类自适应 Schema |
| 2 | 市场洞察 | 趋势、定价区间、毛利预估、客群画像、长尾词 | 旗舰模型 + RAG 增强 |
| 3 | 爆款对标 | 知识库检索分品类爆款标题公式与流量词策略 | Listing 据此改写而非直译 |
| 4 | Listing 撰写 | 多平台规范（Amazon/Shopee/TikTok）标题、五点、描述、Search Terms | 严格字数/格式校验，中英对照 |
| 5 | AI 商品摄影 | 搬运模式沿用原素材（0 生成消耗）；无素材时 Wan2.7 全量生图 | 虚拟试穿/补场景图为按需增值服务 |
| 6 | 带货视频 | LLM 分镜脚本 → TTS 目标语言配音 → ffmpeg 合成 | 无 ffmpeg 自动故事板模式 |
| 7 | 图片本地化 | 详情图中文 → 目标语言（图翻优先，VL 兜底） | 原图/译后对比展示 |
| 8 | 平台合规 | 标题字节数/违禁词/属性完整度逐项校验 | 输出标准化发布包 + SKU |
| 9 | 成果总结 | 全链路报告与发布包导出 | 自动存档任务历史 |

### 2.3 工作流与技能 hub（生态范式工作台）

工作台采用与阿里生态（牛顿/Accio）一致的「工作流和技能」组织范式，评委可直观看到我们原生使用生态语言组织产品能力：

- **侧边栏四视图**：新任务（执行工作台）/ 工作流和技能 / 任务管理 / 连接；
- **工作流卡片**：全链路智能上新、批量上新流水线、市场洞察速览，点「使用」预置意图一键启动；
- **技能卡片**：9 节点原子能力独立陈列。带货视频生产、图片文字本地化为**可选技能**，开关真实控制后端 LangGraph 路由（关闭即跳过该节点，plan 与 ETA 同步更新）；核心节点常开；虚拟试穿为按需增值服务；
- **自定义技能（插件扩展点）**：「我创建的」页签支持卖家自建技能（名称 + 指令模板，支持 {context} 占位符注入商品背景），由 LLM 真实执行并可随时删除——平台能力不封闭，具备 Agent 插件生态雏形；
- **深浅双主题**：CSS 变量体系 + Element Plus 暗色变量联动，侧边栏一键切换并持久化，适配展会投屏与日常办公两种场景；
- **单屏工作台**：执行拓扑可折叠，双面板内部独立滚动，1080p 下整页无滚动条，演示时一屏尽收；
- **连接页**：百炼 / 1688 官方 API / Amazon SP-API / 电商图翻 / ffmpeg 五条外部连接的启用状态与降级策略实时可见。

### 2.4 卖家日常工作流能力

- **批量上新**：CSV 导入（中英文表头兼容）→ SSE 逐商品进度推送 → 自动存档
- **版本管理**：每次上新自动存档 Listing 版本，双版本字段级对比
- **任务历史**：SQLite 持久化，随时回看任意历史任务完整产出
- **断点续跑**：任意节点失败可携带 thread_id 从检查点恢复
- **OAuth 直连发布**：配置 Amazon SP-API 凭证后真实上架；未配置输出完整演练回执

### 2.5 体验方式

```
本地运行：项目根目录双击 start.bat
前端工作台：http://localhost:5173    后端 API 文档：http://localhost:8000/docs
端到端自测：cd backend && python -X utf8 test_pipeline.py
```

---

## 三、技术架构与模型使用说明

### 3.1 总体架构（四层）

```
前端工作台层   Vue 3 + Element Plus（阶段泳道 / 实时拓扑 / 成果多 Tab 大盘）
      │  SSE 流式协议（plan / node_start / node_update / complete / error）
Agent 编排层   FastAPI + LangGraph StateGraph（9 节点 + MemorySaver 检查点）
      │
模型调用层     阿里云百炼统一接入：文本走 OpenAI 兼容协议，生图/配音走 DashScope 原生接口
      │
数据层         ChromaDB 向量知识库（爆款公式/平台规则）+ SQLite（任务/版本）+ 1688 开放平台
```

### 3.2 阿里云百炼模型调用说明

| 模型 | 用途 | 调用方式 |
|------|------|---------|
| Qwen 旗舰（市场洞察） | 跨境市场趋势、定价与客群分析 | OpenAI 兼容 chat |
| Qwen 高端多模态（属性/文案） | 商品图视觉识别、多平台 Listing 生成 | OpenAI 兼容 chat（含图片输入） |
| Qwen 快速（合规校验） | 平台规则逐项质检，低延迟 | OpenAI 兼容 chat |
| Wan2.7-Image-Pro（生图） | 白底主图、海外生活方式场景图 | DashScope 原生接口（同步） |
| Qwen-Audio TTS（配音） | 带货视频目标市场语言口播 | DashScope 原生接口（同步） |
| Embedding/Rerank（检索） | 爆款知识库语义检索与精排 | 向量化 + 本地 ChromaDB |

**工程要点**：
- 专属 API Key 与专属基地址配套，合规抵扣赛事算力额度；
- 全链路**多级降级保障**：生图失败→预设素材、图翻未配置→VL 识别+翻译、ffmpeg 缺失→故事板模式、1688 官方 API 失败→页面抓取兜底——任何单点故障不阻断上新主链路；
- 文本对话支持流式输出，工作台实时进度 + ETA 估算，照顾用户等待体验。

### 3.3 技术栈清单

| 层 | 技术 |
|----|------|
| Agent 编排 | LangGraph StateGraph + Checkpointer |
| 后端 | FastAPI、SSE-Starlette、httpx、Pydantic Settings |
| 前端 | Vue 3、Vite、Element Plus |
| 数据 | ChromaDB、SQLite |
| 媒体 | ffmpeg（视频合成）、Pillow |

---

## 四、项目开发及阶段成果说明

| 阶段 | 周期 | 成果 |
|------|------|------|
| **Phase 1 · MVP** | 已完成 | 6 节点核心链路：属性识别→洞察→Listing→生图→合规→发布包；SSE 可视化工作台；断点续跑 |
| **Phase 2 · 深度内容** | 已完成 | 爆款对标 RAG（新增节点）、详情页图片本地化（新增节点）、全品类属性 Schema |
| **Phase 3 · 真实生产力** | 已完成 | 真实生图（Wan2.7）与 TTS 配音、带货视频节点、OAuth 直连发布、批量上新、版本对比——全链路升级为 9 节点 |
| **产品策略迭代** | 复赛前 | ① 虚拟试穿移出主链路，改为服装类按需增值服务（基于搬运场景调研：多数卖家素材现成）；② 确立"搬运优先、AI 按需补给"策略，搬运模式零生图消耗；③ 1688 一键搬运（链接解析→智能推荐平台→一键启动）+ 1688 开放平台官方 API 通道 |

**与初赛方案的演进说明**：初赛提出的虚拟试穿亮点已落地并重新定位——基于对搬运场景的调研，试穿从主链路收敛为按需增值服务（能力完整保留：三级降级链，独立接口 `/api/products/studio/tryon`），主链路因此提速且全品类通用。这是产品从"功能堆砌"走向"场景驱动"的一次主动迭代。

---

## 五、复赛提交清单对照

| 提交要求 | 状态 | 位置 |
|---------|------|------|
| 可运行产品 Demo | ✅ | 本地一键启动（start.bat），端到端测试通过 |
| 产品功能与使用说明 | ✅ | 本材料第二章 + README |
| 技术架构及模型使用说明 | ✅ | 本材料第三章 |
| GitCode 代码仓库 | 🔧 准备中 | 仓库创建后填入地址 |
| 产品演示视频 | 🔧 准备中 | 录制脚本已排期 |
| 测试账号/体验方式 | ✅ | 本地体验（见 2.4），演示视频覆盖全流程 |
| 项目开发及阶段成果说明 | ✅ | 本材料第四章 |
