# GloLaunch AI — 提交准备指南

## 一、代码仓库推送（GitCode + GitHub）

### 1. 创建远程仓库

**GitCode**（https://gitcode.com）
- 登录后点击「新建仓库」
- 仓库名：`GloLaunch_AI`
- 设为公开（比赛评审需要访问）
- 创建后复制仓库地址，格式类似：`https://gitcode.com/你的用户名/GloLaunch_AI.git`

**GitHub**（https://github.com）
- 登录后点击右上角 `+` → `New repository`
- 仓库名：`GloLaunch_AI`
- 设为 Public
- 创建后复制仓库地址：`https://github.com/你的用户名/GloLaunch_AI.git`

### 2. 推送代码

在项目根目录执行（已自动完成 git init + commit）：

```bash
cd E:\GloLaunch_AI_

# 添加远程仓库（替换为你的实际地址）
git remote add gitcode https://gitcode.com/你的用户名/GloLaunch_AI.git
git remote add github https://github.com/你的用户名/GloLaunch_AI.git

# 推送到两个平台
git push -u gitcode master
git push -u github master
```

### 3. 验证

- 打开 GitCode/GitHub 仓库页面，确认文件列表完整
- 确认 `.env` 文件**没有**被上传（应在 .gitignore 中排除）
- 确认 README.md 在仓库首页正常渲染

---

## 二、产品演示视频脚本

### 录制工具推荐

| 工具 | 特点 | 适用场景 |
|------|------|----------|
| OBS Studio | 免费开源，功能强大 | 全屏录制 + 摄像头画中画 |
| Bandicam | 简单易用，水印小 | 快速录制 |
| Windows 自带 Xbox Game Bar | Win+G 直接录 | 最快捷，无需安装 |
| Loom | 浏览器插件，自动上传 | 快速分享 |

### 录制设置

- 分辨率：1920x1080（1080p）
- 帧率：30fps 即可
- 浏览器缩放：125%（让 UI 元素更清晰）
- 录制前关闭无关通知和标签页

### 演示脚本（约 5-8 分钟）

---

**【开场 — 30秒】**

画面：桌面 / PPT 标题页

旁白：
> 大家好，这是 GloLaunch AI — 全链路跨境智能上新引擎。
> 传统跨境电商上新，从选品到上架平均需要 3-7 天，涉及多个工具、多个平台、大量人工操作。
> GloLaunch AI 的目标是：一张商品图片，30 分钟完成全平台上架。

---

**【场景一：商品导入 — 1分钟】**

画面：打开前端工作台 → 侧边栏工作台视图

操作步骤：
1. 启动项目，打开浏览器访问 http://localhost:8000（或部署后的地址）
2. 展示工作台界面：左侧牛顿风格侧边栏，右侧阶段泳道
3. 在输入框输入：「帮我将这款夏季法式复古碎花连衣裙上架到 Amazon US」
4. 上传一张商品图片（或粘贴 1688 链接）
5. 选择目标平台 Amazon，点击开始

旁白：
> 这是 GloLaunch AI 的工作台。左侧是导航栏，右侧是 6 大阶段的流水线可视化。
> 我现在输入一段自然语言需求，上传商品图片，系统就会自动启动全链路。

---

**【场景二：全链路执行 — 2分钟】**

画面：工作台实时执行过程

重点展示：
1. 节点拓扑图逐个高亮（12 个节点依次激活）
2. 阶段进度条推进
3. 每个节点完成时的 SSE 事件推送
4. 总进度条从 0% 到 100%

旁白：
> 系统由 LangGraph 状态机驱动，12 个智能节点按条件路由自动编排。
> 首先是商品理解阶段，Qwen-VL 多模态模型从图片中提取品类、材质、颜色等结构化属性。
> 接着是市场洞察，结合本地知识库进行 RAG 增强推理，输出目标市场趋势和定价建议。
> 然后是上架机会评分、素材盘点、爆款对标研究...
> 可以看到节点拓扑图上每个节点在依次激活，进度条在实时推进。

---

**【场景三：成果展示 — 2分钟】**

画面：多 Tab 成果大盘

重点展示：
1. Listing Tab：英文标题、五点描述、长描述、Backend Keywords
2. 市场报告 Tab：定价建议、毛利率、高转化关键词
3. AI 拍摄 Tab：白底主图、海外场景图
4. 发布包 Tab：合规报告、SKU、Listing 质量评分

旁白：
> 全链路执行完成后，成果自动分类展示在 4 个 Tab 中。
> Listing Tab 展示了符合 Amazon A9 算法规范的高转化文案，标题 195 字符，五点描述以大写卖点标签开头。
> 市场报告展示了定价建议和 7 个高转化 SEO 关键词。
> AI 拍摄 Tab 展示了白底主图和 3 组欧美生活方式场景图，全部由 Wan2.7 模型生成。
> 发布包 Tab 展示了合规质检结果和 Listing 质量评分，当前是 B 级 76 分。

---

**【场景四：技能 Hub 与可选能力 — 1分钟】**

画面：工作流和技能 Hub 页面

操作步骤：
1. 点击侧边栏「工作流和技能」
2. 展示工作流卡片（全链路上新、仅市场洞察、仅 Listing）
3. 展示技能卡片（带货视频、图片本地化）
4. 关闭「带货视频」技能，说明关闭后全链路会跳过视频生产节点

旁白：
> 这是工作流和技能 Hub。工作流卡片可以一键启动不同意图的流水线。
> 技能卡片控制可选节点。比如带货视频和图片本地化，关闭后全链路会自动跳过这些节点，节省执行时间和 API 额度。
> 这种设计让用户可以根据实际需求灵活裁剪流水线。

---

**【场景五：1688 一键搬运 — 1分钟】**

画面：导入弹窗

操作步骤：
1. 点击「从 1688 导入」
2. 粘贴一个 1688 商品链接
3. 系统自动解析标题、主图、价格
4. 智能推荐目标平台（如：家居类推荐 Shopee 东南亚）
5. 点击「一键上新」

旁白：
> 除了手动输入，GloLaunch AI 还支持 1688 一键搬运。
> 粘贴链接后，系统自动解析商品信息，并根据品类智能推荐最适合的目标平台。
> 全程零手填，傻瓜式操作。

---

**【场景六：技术架构 — 30秒】**

画面：架构图 / README.md 的拓扑图

旁白：
> 技术架构上，后端采用 FastAPI + LangGraph StateGraph，12 个智能节点通过条件路由自动编排。
> LLM 层调用阿里云百炼 Token Plan 的 Qwen3.8-Max、Qwen3.7-Plus、Qwen3.6-Flash 三个模型，分别负责深度推理、多模态文案和快速校验。
> 图像生成用 Wan2.7，语音合成用 Qwen-Audio-TTS，向量检索用 ChromaDB。
> 前端是 Vue 3 + Element Plus，提供实时节点拓扑可视化和阶段泳道进度展示。

---

**【收尾 — 30秒】**

画面：回到工作台首页 / 项目 Logo

旁白：
> GloLaunch AI 将传统 3-7 天的跨境上新流程压缩到 30 分钟，覆盖商品理解、市场洞察、内容生产、合规发布全链路。
> 项目代码已开源在 GitCode 和 GitHub，欢迎体验。
> 谢谢观看。

---

### 录制注意事项

1. **提前跑通一次**：录制前先完整执行一次全链路，确保 API 正常、无报错
2. **准备备用素材**：如果全链路执行时间较长，可以提前录制好加速版，剪辑时快进
3. **突出 SSE 实时效果**：节点逐个激活的过程是亮点，要完整展示
4. **展示真实数据**：用真实商品图片和链接，不要全程用 mock 数据
5. **控制总时长**：5-8 分钟最佳，超过 10 分钟评委可能失去耐心
6. **添加字幕**：如果条件允许，用剪映或 PR 添加关键旁白字幕

---

## 三、阿里云部署方案（体验地址）

### 方案：ECS 轻量应用服务器

大学生优惠券通常可以免费领取或低价购买轻量应用服务器，适合部署 Demo。

### 1. 服务器配置建议

| 配置项 | 推荐值 |
|--------|--------|
| 系统 | Ubuntu 22.04 LTS |
| CPU | 2 核 |
| 内存 | 4 GB |
| 带宽 | 3-5 Mbps |
| 系统盘 | 40 GB |

### 2. 部署步骤

```bash
# SSH 登录服务器后

# 1. 安装环境
sudo apt update && sudo apt install -y python3.11 python3.11-venv python3-pip nodejs npm nginx git

# 2. 克隆代码
cd /opt
sudo git clone https://github.com/你的用户名/GloLaunch_AI.git
cd GloLaunch_AI

# 3. 后端部署
cd backend
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
nano .env  # 填入真实 API Key

# 启动后端（使用 systemd 或 supervisor 保持运行）
uvicorn app.main:app --host 0.0.0.0 --port 8000

# 4. 前端构建
cd /opt/GloLaunch_AI/frontend
npm install
npm run build

# 5. Nginx 配置
sudo nano /etc/nginx/sites-available/glolaunch
```

Nginx 配置示例：

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 或服务器公网 IP

    # 前端静态文件
    location / {
        root /opt/GloLaunch_AI/frontend/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        
        # SSE 支持
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
    }
}
```

```bash
# 启用配置
sudo ln -s /etc/nginx/sites-available/glolaunch /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

# 6. 使用 systemd 保持后端运行
sudo nano /etc/systemd/system/glolaunch.service
```

systemd 服务配置：

```ini
[Unit]
Description=GloLaunch AI Backend
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/opt/GloLaunch_AI/backend
Environment="PATH=/opt/GloLaunch_AI/backend/venv/bin"
ExecStart=/opt/GloLaunch_AI/backend/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
# 启动服务
sudo systemctl daemon-reload
sudo systemctl enable glolaunch
sudo systemctl start glolaunch
sudo systemctl status glolaunch
```

### 3. 域名与 HTTPS（可选）

如果有域名：
1. 在域名服务商处添加 A 记录指向服务器 IP
2. 使用 Let's Encrypt 免费证书：
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

如果无域名：直接用服务器公网 IP 访问即可。

### 4. 安全组配置

在阿里云控制台 → 轻量应用服务器 → 防火墙，开放以下端口：
- 80（HTTP）
- 443（HTTPS，如配置了证书）

### 5. 验证

访问 `http://服务器IP` 或 `http://your-domain.com`，确认：
- 前端页面正常加载
- 后端 API 可访问（`http://服务器IP/api/system/health`）
- SSE 流式推送正常工作

---

## 四、提交清单

| 提交物 | 状态 | 说明 |
|--------|------|------|
| 可运行的 Demo | ✅ 就绪 | 本地 `start.bat` 一键启动 |
| 产品功能与使用说明 | ✅ 就绪 | README.md + QUICKSTART.md |
| 技术架构及模型说明 | ✅ 就绪 | README.md 技术架构章节 |
| 代码仓库地址 | ⏳ 待推送 | 按本文第一节操作 |
| 产品演示视频 | ⏳ 待录制 | 按本文第二节脚本操作 |
| 测试账号/体验地址 | ⏳ 待部署 | 按本文第三节操作 |
| 开发及阶段成果说明 | ✅ 就绪 | 参赛方案 + 复赛材料 + 审计报告 |
